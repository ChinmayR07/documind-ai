# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0           # Modern async Python web framework
uvicorn[standard]==0.29.0  # ASGI server to run FastAPI
python-multipart==0.0.9    # Required for file upload handling

# ─── AI ───────────────────────────────────────────────────────────────────────
anthropic==0.28.0          # Official Claude API client

# ─── Document Parsing ─────────────────────────────────────────────────────────
PyMuPDF==1.24.3            # PDF parsing (imported as 'fitz')
python-docx==1.1.0         # Word document (.docx) parsing
Pillow==10.3.0             # Image processing
pytesseract==0.3.10        # OCR — extracts text from images

# ─── Caching ──────────────────────────────────────────────────────────────────
redis==5.0.4               # Redis client for caching
hiredis==2.3.2             # Fast Redis parser (optional but recommended)

# ─── Data Validation ──────────────────────────────────────────────────────────
pydantic==2.7.1            # Data validation (FastAPI uses this internally)
pydantic-settings==2.2.1   # Settings management from env vars

# ─── Utilities ────────────────────────────────────────────────────────────────
python-magic==0.4.27       # Detect real file type (not just extension)
aiofiles==23.2.1           # Async file I/O
httpx==0.27.0              # Async HTTP client (for health checks)
python-jose==3.3.0         # JWT tokens (for future auth)
